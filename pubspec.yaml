name: authentication
description: an authentication system that allow user register, login and reset password

publish_to: 'none' # Remove this line if you wish to publish to pub.dev


version: 1.0.0+1

environment:
  sdk: '>=2.18.1 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.3
  google_fonts: ^3.0.1
  get_storage: ^2.0.3
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  flutter_svg: ^1.1.1+1
  http: ^0.13.4
  get: ^4.6.5

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  hive_generator: ^1.1.3
  build_runner: ^2.1.11
  uuid: ^3.0.6
  pin_code_fields: ^7.4.0

  flutter_lints: ^2.0.0

flutter:


  uses-material-design: true

  assets:
     - assets/images/
     - assets/svg/

